{
  "name": "my-api-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "docusaurus clear && node scripts/generate-askai-index.mjs && node scripts/generate-section-pdfs.mjs",
    "prebuild": "docusaurus clear && node scripts/generate-askai-index.mjs && node scripts/generate-section-pdfs.mjs",
    "predeploy": "docusaurus clear && node scripts/generate-askai-index.mjs && node scripts/generate-section-pdfs.mjs",
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "npm run start",
    "serve:static": "npm run build && docusaurus serve",
    "pdf": "node scripts/generate-section-pdfs.mjs",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "3.9.2",
    "@docusaurus/preset-classic": "3.9.2",
    "@easyops-cn/docusaurus-search-local": "^0.55.0",
    "@mdx-js/react": "^3.0.0",
    "@monaco-editor/react": "^4.7.0",
    "clsx": "^2.0.0",
    "minisearch": "^7.2.0",
    "monaco-editor": "^0.55.1",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.2",
    "@docusaurus/types": "3.9.2",
    "playwright": "^1.53.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
